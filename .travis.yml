language: objective-c
before_install:
	- gem install cocoapods --no-rdoc --no-ri --no-document --quiet
    - cd Tests && pod install && cd $TRAVIS_BUILD_DIR
script: xctool -workspace AWSRequestSigner.xcworkspace -scheme "Unit Tests" test -test-sdk iphonesimulator7.0